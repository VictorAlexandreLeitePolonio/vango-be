# Copy to an ignored local env file. Never commit populated values.
NOTIFICATION_WORKER_SECRET=
FCM_PROJECT_ID=
FCM_CLIENT_EMAIL=
# PKCS#8 service-account key; the worker accepts escaped newlines.
FCM_PRIVATE_KEY=
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are supplied by the Edge runtime.
